import tkinter as tk
window = tk.Tk()
ROW = 5
COLUMNS = 7

buttons = []
for i in range(ROW):
    temp = []
    for j in range(COLUMNS):
        bth = tk.Button(window)
        temp =append(bth)
    buttons.append(temp)


print()
    





window.mainloop()